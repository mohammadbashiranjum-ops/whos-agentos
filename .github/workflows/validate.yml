name: Validate

on:
  push:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

env:
  UV_SYSTEM_PYTHON: 1

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.14"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: "pyproject.toml"

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install the project with dev dependencies
        run: |
          uv pip install --system -r requirements.txt
          uv pip install --system -e .[dev]

      # scripts/validate.sh owns the whole gate — format, lint, type. Keeping CI to
      # that one call is what makes local and CI the same check by construction.
      - name: Validate (format + lint + type)
        run: uv run bash scripts/validate.sh
